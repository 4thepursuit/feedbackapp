<!DOCTYPE html>
<html>
	<head>

  <!--<link rel="shortcut icon" href="public/images/favicon_grumpy_cat_final.ico"/>

<%= favicon_link_tag 'favicon_grumpy_cat_final.ico' %>
  <title>Feedbackapp</title> -->
  
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

<%= render 'layouts/header' %>

<div class="container">
	<% flash.each do |name, msg| %>
	<%= content_tag(:div, msg, class: "alert alert-#{name}") %>
		<% end %>

	<%= yield %>
</div>
</body>
</html>
